dist: xenial
language: node_js
os: linux
matrix:
  include:
    - node_js: '12'
cache:
  directories: [node_modules]
install:
  yarn
services:
  - postgresql
before_script:
  - psql -c 'create database movie_test;' -U postgres
  - sequelize db:migrate
script:
  - npm test
after_success:
  - npm run coverage
